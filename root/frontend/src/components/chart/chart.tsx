import React from 'react';
import { Box } from '@mui/material';

export const Chart = () => {

    return (
        <Box sx={{ backgroundColor: '#fff', height: 250, width: 800, borderRadius: '8px'}}>

        </Box>
    );
};